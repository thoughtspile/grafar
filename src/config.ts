export const config = {
    debug: false,

    minPanelWidth: 600,
    minPanelHeight: 600,
    container: window,
    antialias: true,

    axes: ['x', 'y', 'z'],
    axisLength: 2,

    particleRadius: .05,

    samples: 100,
    tol: 0.001,
    diffStep: 0.001,
}
